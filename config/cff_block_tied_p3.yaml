# config/cff_block_tied_p7 with the slow update every third core cycle (three per pass).
defaults:
  - /cff_block_tied@_here_
  - _self_

run_name: cff_block_tied_p3

arch:
  cff_period: 3
  cff_phase: pre
